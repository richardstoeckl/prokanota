{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Schema for metadata CSV file (validates each row as an object)",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "Sample/genome identifier",
            "minLength": 1
        },
        "path": {
            "type": "string",
            "description": "Path to input FASTA file",
            "minLength": 1
        }
    },
    "required": ["name", "path"]
}
